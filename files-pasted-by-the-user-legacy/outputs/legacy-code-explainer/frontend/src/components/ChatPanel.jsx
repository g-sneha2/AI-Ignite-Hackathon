import {useState} from 'react';
export default function ChatPanel({messages,onAsk}){const [q,setQ]=useState('');return <section className="chat"><h2>Ask about this codebase</h2><div className="messages">{messages.map((m,i)=><div className={m.role} key={i}><b>{m.role==='user'?'You':'Explorer'}</b><p>{m.text}</p>{m.sources&&<small>Sources: {m.sources.join(', ')}</small>}</div>)}</div><form onSubmit={e=>{e.preventDefault();if(q.trim()){onAsk(q);setQ('')}}}><input value={q} onChange={e=>setQ(e.target.value)} placeholder="How does this project start?"/><button>Ask</button></form></section>}
