import {useState} from 'react';
export default function ChatPanel({messages,onAsk,loading}){const [q,setQ]=useState('');return <section className="chat"><h2>Ask about this codebase</h2>{loading&&<div className="loader">Explorer is thinking…</div>}<div className="messages">{messages.map((m,i)=><div className={m.role} key={i}><b>{m.role==='user'?'You':'Explorer'}</b><p>{m.text}</p>{m.sources&&<small>Sources: {m.sources.join(', ')}</small>}</div>)}</div><form onSubmit={e=>{e.preventDefault();if(q.trim()&&!loading){onAsk(q);setQ('')}}}><input value={q} onChange={e=>setQ(e.target.value)} placeholder="How does this project start?" disabled={loading}/><button disabled={loading}>{loading?'Waiting...':'Ask'}</button></form></section>}
