import {useState} from 'react';
export default function RiskWarning({functions=[],onCheck,data,loading}){const [fn,setFn]=useState('');return <section><h2>Change risk check</h2>{loading&&<div className="loader">Explorer is checking risk…</div>}<div className="inline"><select value={fn} onChange={e=>setFn(e.target.value)} disabled={loading}><option value="">Choose a function</option>{functions.map(f=><option key={f.name}>{f.name}</option>)}</select><button disabled={!fn||loading} onClick={()=>onCheck(fn)}>{loading?'Checking...':'Check risk'}</button></div>{data&&<div className="warning"><p>{data.warning}</p><small>Dependents: {data.dependents.join(', ')||'none detected'}</small></div>}</section>}
