export default function ExplanationPanel({data,loading}) { if(loading)return <p className="loader">Explorer is generating an explanation…</p>; if(!data)return <p>Select a Python file to explain it.</p>;return <section><h2>Plain-English explanation</h2><p className="prose">{data.explanation}</p><h3>Functions</h3>{data.functions.map(f=><div className="card" key={f.name}><b>{f.name}</b><span>{f.summary}</span></div>)}<h3>Imports</h3><p>{data.imports.join(', ')||'None detected'}</p><h3>Called by</h3><p>{data.called_by.join(', ')||'No direct calls detected'}</p></section> }
